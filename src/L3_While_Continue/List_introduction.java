package L3_While_Continue;

public class List_introduction {
    public static void main(String[] args) {
        //List type creating
        // 1) when it is filled in
        int[] numbers = {3, 6, 5, 8, 46, 23};
        for(int i = 0; i < numbers.length; i++){ //numbers.length - length of list
            System.out.println(numbers[i]);

        }
    }
}
